[package]
name = "apple-sdk-port"
description = "SDKs for Apple Platform(macOS/iOS) port for Rust"
version = "0.1.0"
edition = "2024"

[features]
cargo-clippy = [] # disable warning inside objc crate

[dependencies]
objc = "0.2"
