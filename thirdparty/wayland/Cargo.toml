[package]
name = "peridot-tp-wayland"
version = "0.1.0"
edition = "2024"

[features]
default = []
tracing = ["dep:tracing"]

[dependencies]
bitflags = "2.9.3"
tracing = { version = "0.1", optional = true }
