[package]
name = "prof-reader"
version = "0.1.0"
edition = "2024"

[dependencies]

[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.62", features = ["Win32_Storage_FileSystem", "Win32_System_Memory", "Win32_Security"] }
windows-core = { version = "0.62" }

[lints]
workspace = true
